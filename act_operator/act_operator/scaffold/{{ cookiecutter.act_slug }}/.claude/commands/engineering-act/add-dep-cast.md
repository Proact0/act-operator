# Add Cast Dependency

Add dependency to a specific cast only.

```bash
uv add --package <cast> <package>      # Add
uv remove --package <cast> <package>   # Remove
```

After adding, update `casts/<cast>/CLAUDE.md` Technology Stack section.
